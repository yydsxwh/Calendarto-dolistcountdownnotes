import { useEffect, useState } from 'react'
import { collectDueReminders, takeUnfired, upcomingReminderSlots, type DueReminder } from '../lib/reminders'
import { isNativeApp } from '../lib/native'
import type { CalendarEvent, Course, Exam, RecurringReminder, ReminderSettings, SelfScheduleItem, Todo } from '../types'
function notifId(key:string):number{let hash=0;for(let i=0;i<key.length;i++)hash=(hash*31+key.charCodeAt(i))|0;return Math.abs(hash)%2147483646||1}
async function notifyNative(item:DueReminder){const{LocalNotifications}=await import('@capacitor/local-notifications');await LocalNotifications.schedule({notifications:[{id:notifId(item.key),title:item.title,body:item.body,schedule:item.fireAt>Date.now()+2000?{at:new Date(item.fireAt)}:undefined,extra:{key:item.key,kind:item.kind}}]})}
async function syncNativeSchedule(courses:Course[],exams:Exam[],settings:ReminderSettings,todos:Todo[],selfSchedules:SelfScheduleItem[],events:CalendarEvent[],recurring:RecurringReminder[]):Promise<void>{const{LocalNotifications}=await import('@capacitor/local-notifications');const pending=await LocalNotifications.getPending();if(pending.notifications.length)await LocalNotifications.cancel({notifications:pending.notifications.map(n=>({id:n.id}))});const slots=upcomingReminderSlots(courses,exams,settings,Date.now(),2,todos,selfSchedules,events,recurring);if(!slots.length)return;await LocalNotifications.schedule({notifications:slots.map(item=>({id:notifId(item.key),title:item.title,body:item.body,schedule:{at:new Date(item.fireAt)},extra:{key:item.key,kind:item.kind}}))})}
export function useReminders(courses:Course[],exams:Exam[],settings:ReminderSettings,todos:Todo[]=[],selfSchedules:SelfScheduleItem[]=[],events:CalendarEvent[]=[],recurring:RecurringReminder[]=[]){const[banner,setBanner]=useState<DueReminder|null>(null);useEffect(()=>{if(!settings.enabled)return;const tick=()=>{const due=takeUnfired(collectDueReminders(courses,exams,settings,Date.now(),todos,selfSchedules,events,recurring));if(!due.length)return;setBanner(due[0]);if(isNativeApp()){due.forEach(item=>void notifyNative({...item,fireAt:Date.now()}));return}if(typeof Notification!=='undefined'&&Notification.permission==='granted')due.forEach(item=>{try{new Notification(item.title,{body:item.body,tag:item.key})}catch{/* 通知被系统或用户拒绝时照常走应用内横幅 */}})};tick();const id=window.setInterval(tick,30000);return()=>window.clearInterval(id)},[courses,exams,settings,todos,selfSchedules,events,recurring]);useEffect(()=>{if(!isNativeApp()||!settings.enabled)return;void syncNativeSchedule(courses,exams,settings,todos,selfSchedules,events,recurring)},[courses,exams,settings,todos,selfSchedules,events,recurring]);const requestPermission=async()=>{if(isNativeApp()){const{LocalNotifications}=await import('@capacitor/local-notifications');const perm=await LocalNotifications.requestPermissions();return perm.display==='granted'?('granted' as const):('denied' as const)}if(typeof Notification==='undefined')return'denied' as const;if(Notification.permission==='granted')return'granted' as const;return Notification.requestPermission()};const preview=(item:DueReminder)=>{setBanner(item);if(isNativeApp()){void notifyNative({...item,fireAt:Date.now()});return}if(typeof Notification!=='undefined'&&Notification.permission==='granted')try{new Notification(item.title,{body:item.body,tag:item.key})}catch{/* 通知被系统或用户拒绝时照常走应用内横幅 */}};return{banner,dismiss:()=>setBanner(null),requestPermission,preview}}
